<%= form_for @slog, :html => { :multipart => true } do |f| %>
    <div class="slog_featured_image">
        <h3>Featured Image</h3>
        <%= slog_image(@slog) %>
        <div><%= f.file_field :featured_image %></div>
    </div>
    <div class="slog_attr">
        <div class="form_text">
            <%= f.label :title %><%= f.text_field :title, :placeholder => "Title", :required => true %>
            <%= f.label :tag_list, "Tags"%><%= f.autocomplete_field :tag_list, autocomplete_tag_name_slogs_path, :placeholder => "Tags", :"data-delimiter" => ', ' %>
            <%= f.label :slog_type %><%= f.collection_select :slog_type_id, SlogType.all, :id, :name %>
            <%= f.label :mission %><%= f.collection_select :mission_id, current_user.missions, :id, :name %>
        </div>
    </div>
    <div class="form_blocks">
      <%= f.fields_for :slog_blocks do |b| %>
        <fieldset>
          <%= b.label :image, "Block Image" %>
          <%= b.file_field :image %>
          <%= b.label :body, "Block Text" %>
          <%= b.text_area :body %>
        </fieldset>
      <% end %>        
    </div>
    <div class="form_actions"><%= f.submit nil, :class => 'btn btn-primary' %></div>
<% end %>

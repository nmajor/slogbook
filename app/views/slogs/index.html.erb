<section class="home">
    <section class="slog_list">
      <%= will_paginate @slogs %>
        <% @slogs.each do |slog| %>
            <%= render "slog_item", :slog => slog %>
        <% end %>
      <%= will_paginate @slogs %>
    </section>
    <section class="means">
        <%= render "shared/sidebar" %>
    </section>
</section>

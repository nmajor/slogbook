<section class="home">
    <section class="page_body">
      <section class="slog_list">
        <%= will_paginate @slogs %>
          <% @slogs.each do |slog| %>
              <%= render "slog_item", :slog => slog %>
          <% end %>
        <%= will_paginate @slogs %>
      </section>
    </section>
    <%= render "shared/sidebar" %>
</section>
